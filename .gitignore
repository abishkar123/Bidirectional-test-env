## .NET
bin/
obj/
*.user
*.suo
.vs/
_ReSharper*/
*.DotSettings.user

## Secrets — never commit
appsettings.Local.json
appsettings.*.Local.json
secrets.json

## Build artifacts
publish/
*.nupkg
*.snupkg
TestResults/
coverage/

## OS
.DS_Store
Thumbs.db

## Terraform
.terraform/
*.tfstate
*.tfstate.*
*.tfplan
tfplan
override.tf
override.tf.json

## Terraform variable files — contain environment-specific IDs, never commit.
## Use terraform.tfvars.example as the committed template.
terraform.tfvars
*.auto.tfvars

## Per-tenant backend configs — contain subscription/storage account names.
## Use backend-configs/example.conf.example as the committed template.
backend-configs/*.conf
